#include <stdio.h>
#include <stdint.h>
#include <errno.h>
// #include "chip8.h"

int main()
{
    FILE *fpointer;
    // fpointer = fopen("DataFlair.txt", "r"); // Opening the file
    printf(" The value of errno is: %d\n", errno);

    return 0;
}